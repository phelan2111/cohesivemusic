export enum ModalView {}
export enum Langue {
	vietNamese = 0,
	english,
}

export enum TypeToTry {
	single,
	album,
}

export enum Gender {
	female = 0,
	male,
}

export enum StatusPlaylist {
	hidden = 0,
	display,
	user,
}
